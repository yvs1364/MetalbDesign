<div class="container" style="margin-top: 5% !important">
  <%= link_to "retour", root_path, class: "btn btn-dark" %>
  <div class="col-md-6">
    <h1>Ajouter un nouveau meuble</h1>

    <%= simple_form_for (item) do |f| %>
    <%= f.input :name %>
    <%= f.input :description, as: :text, :input_html => { class: 'test', :cols => 5, :rows => 10 } %>
    <%= f.input :photos, as: :file, input_html: { multiple: true } %>
    <%= f.button :submit, class: "btn btn-primary", value: "Créer" %>
    <% end %>
  </div>
</div>
